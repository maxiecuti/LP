```mermaid

flowchart TD
  inicio(( Início )) --> entrada[/Ler número N/]
  entrada --> decisaoInicial{N <= 1?}
  decisaoInicial -- Sim --> naoPrimo[Número não é primo]
  decisaoInicial -- Não --> init[i = 2]

  init --> loop{i <= N - 1?}
  loop -- Sim --> divisao{N % i == 0?}
  divisao -- Sim --> naoPrimo
  divisao -- Não --> incremento[i = i + 1]
  incremento --> loop

  loop -- Não --> primo[Número é primo]

  primo --> fim(( Fim ))
  naoPrimo --> fim
